# Aspose.Cells for .NET – Agentic Examples

This repository contains automatically generated **Aspose.Cells for .NET** code examples.

Examples are generated and validated by the **Aspose.Cells Product Agent** using the Professionalize multi-agent architecture.

## Architecture

The generation pipeline uses multiple agents:

- **Task Browser Agent (7001)**  
  Provides product categories and tasks.

- **Examples Super Agent (7050)**  
  Generates implementation code examples.

- **Aspose.Cells Product Agent**  
  Builds, validates, and publishes examples.

Only successfully validated examples are published to this repository.

## Repository Structure

Examples are organized by **feature category**.

Example folders:

Each category folder contains:

- Example `.cs` files
- `agents.md` listing tasks implemented in that category

## Example Format

Each example is a **complete .NET console program** demonstrating a specific Aspose.Cells feature.

---

Generated automatically by the Aspose.Cells Product Agent.
